{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build and run",
      "type": "shell",
      "command": "cmd /c 'g++ -o ${fileBasenameNoExtension}.exe ${fileBasename} && .\\${fileBasenameNoExtension} < input.inp > output.out'",
      "problemMatcher": [],
      "presentation": {
        "reveal": "silent",
        "showReuseMessage": false
      },
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}